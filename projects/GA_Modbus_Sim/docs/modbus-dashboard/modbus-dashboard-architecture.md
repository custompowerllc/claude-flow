Is it better to have the standalone modbus logger save to csv file. Then

Option 1:
 Have the modbus dashboard app read the csv file and monitor changes and update the gui. 

Option 2:
 Have the standalone modbus logger open websocket communication and stream data to the modbus dashboard which updates the gui